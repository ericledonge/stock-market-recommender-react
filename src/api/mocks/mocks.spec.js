import { stockPriceGenerator } from './mocks';

describe('stockPriceGenerator', () => {
  const STOCK_SYMBOL = 'GOOG';
  const

    describe;
  ('when duration is 1 day', () => {
    it('returns a table with an object with date is today and value is random between 100 and 200', () => {
      const stockPrices = stockPriceGenerator(STOCK_SYMBOL, 1);
      const expected = [
        { date: }
      ];
      expect(stockPrices).toEqual();
    });
  });


});
